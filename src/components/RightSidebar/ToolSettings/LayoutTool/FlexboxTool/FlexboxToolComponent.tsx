import React from "react";

import FlexDirection from "./FlexDirection";

type Props = {};

const FlexboxToolComponent = (props: Props) => {
  return (
    <div>
      <FlexDirection />
    </div>
  );
};

export default FlexboxToolComponent;
